use dev metrics as template: title, header, filters, cards

start date and end date always use presets...
if using dates (startas filters, add a clear dates with presets
one pair of dates: with the presets....
two pair of dates: with the other presets...

work items --> use filter by area path, disable filter by author
pull requests --> use filter by author, disable filter by area path
what about sonar data?

